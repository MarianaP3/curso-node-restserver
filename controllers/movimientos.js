/* eslint-disable camelcase */
// const { response, request } = require('express')

// const Movimiento = require('../models/movement')
// const Usuario = require('../models/user')

// GET METHODS
/* const getMovementsByUser = async (req = request, res = response) => {
  const { limit = 5, since = 0 } = req.query
  const query = { user_id }

  const [total, movimientos] = await Promise.all([
    // It runs simultaneously
    Movimiento.countDocuments(query)
      .skip(since)
      .limit(limit)
  ])
} */

// POST METHODS

// PUT METHODS

// DELETE METHODS
